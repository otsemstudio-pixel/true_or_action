// Langues supportées par l'application (interface ET banque de questions).
// Partagé entre l'authentification (users.langue) et le jeu (rooms.langue).
export const SUPPORTED_LANGUES = ['fr', 'en'];

export const DEFAULT_LANGUE = 'fr';
