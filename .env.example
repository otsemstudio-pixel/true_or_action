DATABASE_URL=
PORT=3000
NODE_ENV=development
JWT_SECRET=
# Origine(s) autorisées pour CORS et le handshake Socket.io — une ou
# plusieurs, séparées par des virgules (ex: domaine de prod + variante www).
# Défaut si absent : http://localhost:5173 (à ne JAMAIS laisser en
# production, sous peine de voir le vrai frontend rejeté).
CLIENT_ORIGIN=http://localhost:5173
